# ---------- 多环境（与 package.json 中 --mode 对应）----------
# Vite 会加载：.env、.env.local、.env.[mode]、.env.[mode].local（后者通常 gitignore）
# 例如：npm run build:staging → mode=staging → 读取 .env.staging
#      npm run build / build:prod → mode=production → 读取 .env.production
#
# 登录 / 连接 Weaviate 时出现 ERR_NETWORK（网络错误）的可选提示文案（覆盖 i18n 默认）
# 修改后需重启 dev 或重新执行 npm run build

# 通用回退（开发与生产均可，若下面分环境有值则分环境优先）
# VITE_CONNECTION_NETWORK_MESSAGE=

# 仅本地开发（npm run dev）
# VITE_CONNECTION_NETWORK_MESSAGE_DEVELOPMENT=

# 仅生产构建产物（npm run build、Docker、vite preview）
# VITE_CONNECTION_NETWORK_MESSAGE_PRODUCTION=

# ---------- Docker：Nginx → Node /weaviate 时，前端须带同域代理开关 ----------
# VITE_USE_SAME_ORIGIN_WEAVIATE_PROXY=true
#
# 容器内 Node 代理回退地址（无 X-Weaviate-Target 或非法时使用），对应 dev 的 VITE_WEAVIATE_PROXY_TARGET
# WEAVIATE_PROXY_TARGET=http://127.0.0.1:8080
